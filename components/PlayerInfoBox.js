import {Flex, Box, VStack, HStack} from '@chakra-ui/react'

const PlayerInfoBox = (playerName) => {
    return (
        <Flex
            width={500}
            height={300}
            as="div"
            className='playerinfobox'
        >
        </Flex>
    )
}

export default PlayerInfoBox